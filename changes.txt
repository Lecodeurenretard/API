Ajout doc dans README.md
class.php:
    -ajout sécurité, il lancera une erreur si le fichier n'est pas un .mp3
    -update l206

functions.php:
    -définition des constantes si non définies precedament
    -fix bugs et raccoucit dans redirect()

get-*.php:
    -le module retournera les erreurs et l'outupt (sauf get-music qui ne retourne que les erreurs) même si la méthode HEAD est utilisée
    -utilisation de la méthode ServerError::constructFromThrowable() en cas d'erreur PHP (la méthode existait avant, c'est juste que l'on ne l'utilisait pas)