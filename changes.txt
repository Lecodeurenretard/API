class.php:
    -added "album" field in response,
    -setFromFile() returns now $this,
    -added private function Music::getDefault() which returns MusicdefaultObject (useful for setters)

get-req.php becomes get-json.php

Updates et corrections doc dans README.md