Fixed "control characters glitch" (chars like null (0x00), enq (0x05) or soh (0x01) would be in strings and were messing up the output).
fixed some minor bugs.

functions.php:
	added verifyAndAccept(), a shorthand for a combinaison of verifyAcceptsType()s
	added parseHeader() (not used yet)